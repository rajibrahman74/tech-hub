import React from 'react';

const JobDetailsData = ({data}) => {
    console.log(data);
    return (
        <div>
            <h1>job details data</h1>
        </div>
    );
};

export default JobDetailsData;